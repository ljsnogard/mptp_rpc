[workspace]
resolver = "3"
members = [
    "crates/rpc_core",
    "crates/rpc_transport_iroh",
    "crates/rpc_cs_demo",
]

[workspace.package]
edition = "2024"
authors = ["lino <lino_snsalias@outlook.com>"]

# segm_buff 依赖的 abs_buff 指向 github 镜像（ljsnogard/abs_buff），
# 而本工作区使用 gitee 源（lino_snsalias/abs_buff）。两者若各自解析会形成
# 两个不同的 crate 实例，导致 trait 无法统一；这里把 github 源统一改指到 gitee。
# [patch."https://github.com/ljsnogard/abs_buff.git"]
# abs_buff = { path = "../cantare/crates/abs_buff" }
